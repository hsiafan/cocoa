package coregraphics

import (
	"testing"
)

func TestWindowListCreate(t *testing.T) {
	// windowIDs := WindowListCreate(WindowListOptionOnScreenOnly|WindowListExcludeDesktopElements, NullWindowID)
	// fmt.Println(windowIDs)
}
